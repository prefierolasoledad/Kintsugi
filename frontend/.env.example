# Where the Next.js server reaches the Express backend. Server-side only — this
# is deliberately not a NEXT_PUBLIC_* variable, because the browser must never
# learn the backend's address. All browser traffic goes through the BFF route
# handlers under src/app/api/*. See docs/adr/0002-bff-proxy.md
BACKEND_URL="http://localhost:4000"
