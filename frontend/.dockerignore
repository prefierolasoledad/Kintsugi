# See the note in backend/.dockerignore — the whole context is sent to the
# daemon, so anything not listed here is copied on every build.

node_modules

# Built inside the image. A host .next contains absolute paths from the
# developer's machine and would be silently wrong.
.next
out

.env*.local

npm-debug.log*
.DS_Store
